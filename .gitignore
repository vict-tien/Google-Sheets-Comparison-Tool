# Harness output. All three are regenerated by their scripts; the results that
# matter are recorded in the implementation documentation, where they can be
# read against the code they describe.
out.txt
sabotage.txt
dryrun.txt

# clasp. Holds a script id, which is deployment configuration and not source.
# Copy .clasp.json.example and fill it in; see README "Install".
.clasp.json
.clasprc.json

# Local editor and OS noise.
.vscode/
.idea/
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Claude Code local settings — machine-specific, not shared.
.claude/settings.local.json
